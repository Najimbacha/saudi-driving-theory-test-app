import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.saudi.drivingtheory",
  appName: "Saudi Driving Theory Test",
  webDir: "dist",
  bundledWebRuntime: false,
};

export default config;
